# Changelog

## 0.8.1 - 2026-09-25

- **校验失败时不再只说「输入内容不完整或格式不正确」**，改为指出具体是哪个字段、错在哪。
  - 例如：「标题：缺少必填内容」「标题：内容过长，最多 300 个字」「链接地址：不是有效的网址」「资料 ID：无效（数据可能已损坏）」「资料类型：只能选 file / link / mindmap」。
  - 字段名会翻译成中文（`title` → 标题、`url` → 链接地址、`id` → 资料 ID……），嵌套字段显示完整路径；多个问题用「；」连接，最多显示三条。
  - 起因是上一轮排查时发现：测试夹具用了短 id 而 IPC 要求 UUID，于是保存**静默失败**，界面只给一句通用提示，完全看不出该改什么。
- **跑团记录页的跑团链接不再弹完整地址的悬停窗**。用户反馈「鼠标停留在跑团链接时过一会会自动出现完整地址的悬停窗，这个功能在跑团记录页面没有必要」——那是浏览器原生 `title`，样式是系统黑底方块，与应用风格也不一致。链接地址文字本身仍然显示（过长由省略号截断）。
- **拖拽时的淡紫描边改细，并与卡片外框完全对齐**。
  - 用户反馈「紫色边框太粗，且是圆角矩形，和资料实际拖拽的外框造型和尺寸均不一致，明显略小一圈」。
  - 改用卡片**自身的边框**上色，而不是 2px 的 `inset` 阴影：边框本来就存在（1px 透明），上色不改变盒模型，而且形状（圆角）、尺寸、位置与卡片**天然完全一致**，不会再出现「描边比卡片小一圈」。
  - 线宽与卡片边框同为 1px（此前 2px 的 inset 阴影在 Windows 常见的 125% 缩放下会渲染成 2.5 设备像素，比卡片边框粗一大截）。落点分组的描边同步收到 1px，两者观感成套。
  - 顺带把卡片底部内边距由 8px 收到 4px：此前底部比内容多出一截空白，描边贴着卡片边界画就显得「框小了」。
- 新增测试：`tests/validation-messages-081.test.ts`（10 条，逐个校验场景核对提示文案）、`tests/validation-link-outline-081.test.ts`（8 条）。
- 反向验证：校验改回笼统提示 → 1 条失败；链接恢复 `title` → 2 条失败；描边改回 2px `inset` 阴影 → 1 条失败。

## 0.8.0 - 2026-09-25

- 修复**拖拽资料时出现底色块，而且取决于鼠标抓取的位置**的问题。用户反馈：「拖动后是否有底色块取决于鼠标抓取的位置——鼠标在图标上精准拖拽则无底色块，位置在图标周围则会抓取色块，如果拖拽时出现悬停白字黑底框，则色块宽度和黑底框一致。」
  - 这条线索直接点出了机制。实测确认：`dragstart` 那一刻浏览器会把拖拽源（这张卡片）**截图**作为「拖拽影像」跟着鼠标走。若此刻提示框正显示着，它会连同溢出卡片的部分被一起拍进去——提示框实测 **189px 宽、向上溢出卡片 35px**，而卡片只有 106px 宽，于是那个「色块」又宽又高，正好和提示框一样大。抓图标正中心时提示框不显示（提示只挂在文件名上），所以看着是干净的。
  - 修法：`dragstart` 时**同步**收掉提示框。注意不能靠 React state——`setState` 是异步的，等重渲染完浏览器早就截完图了；这里直接给 `body` 和卡片打 DOM 属性，由 CSS 当帧生效。
  - 同时统一了拖拽外观：正在拖的卡片加淡紫描边、落点分组也是淡紫描边，**不论从哪儿抓显示效果都一致**。描边用 `inset box-shadow` 而不是 `border`，避免改变盒模型让卡片/分组跳动。
- 新建链接资料时，**链接地址不再预填 Notion 占位**，留空让用户自己填。
- 链接类资料没有本地文件，**文件夹图标改为灰色且不可点击**（保留图标与位置，四格不错位），让用户看得出「这里本来有个按钮，只是这类资料用不上」。
- 资料汇总页所有**「笔」图标改为「修改信息」**：点击后打开弹窗，可改标题、归属模组、备注；链接类还能改链接地址，文件类能重新指定文件。
  - 「用原程序打开」没有丢，仍然保留在**双击卡片**上。
- 新增测试：`tests/resources-interactions-080.test.ts`（14 条）与 `tests/e2e/resources-actions-080.spec.ts`（5 条，在真实界面里量拖拽样式、占位图标、编辑弹窗的保存与持久化）。
- 反向验证：dragstart 不收提示框 → 2 条失败；占位改回空 span → 1 条失败；恢复 Notion 占位 → 1 条失败；笔改回「打开原程序」→ 1 条失败。

## 0.7.9 - 2026-09-25

- 修复**全模组正文搜索一输入字符就白屏死机**的问题（严重）。用户反馈「跑团记录页面，全模组正文搜索框，输入任意字符会导致应用死机，只能强行退出」。
  - 用真实运行的应用 + 用户的数据库复现，抓到了渲染进程的报错：
    ```
    TypeError: line.toLocaleLowerCase is not a function
        at searchOutline → moduleOutlineHits → App
    ```
    渲染时抛异常，React 会**卸载整棵组件树**——页面 `innerText` 长度变成 0，用户看到的就是「死机/白屏」。实测：输入第一个字符正常，输入第二个字符就卡住。
  - 根因是**类型不一致**：IPC 的 `resources:read-mindmap` 返回 `outline: Array<{ text, depth }>`（对象数组），而 `App.tsx` 里 `outlineCache` 声明成了 `Record<string, string[]>`，于是把对象数组直接当字符串数组传给了 `searchOutline`。**类型写错让这个 bug 通过了类型检查**，只有运行时才炸。
  - 修法：按真实结构声明类型（`MindmapPreviewApi['outline']`），搜索前取 `.text`；并加一道 `filter` 兜住非字符串，避免以后再出现「一个字段不对就整页白屏」。现在 TypeScript 能拦住这类写法了。
- 修复**拖拽资料时出现的整块底色块**的问题。用户反馈「拖拽资料仍然会出现底色块，有的时候有有的时候没有，出现比较频繁」。
  - 上一版把**卡片**的底色去掉了，但那个色块其实**不是卡片，是整个分组**：落点高亮给分组铺了一层 `--accent-soft`，实测是 **1642×214 像素**的淡紫色块（`rgba(109, 69, 245, 0.12)`），拖动时非常显眼。
  - 改成只加内描边（`box-shadow: inset`），不铺任何底色。用内描边而不是加粗边框，是因为加粗边框会改变盒模型、让分组在拖动时跳动。
- 新增测试：`tests/outline-search-crash-079.test.ts`（5 条，含「把对象当字符串传就会崩」的复现）、`tests/e2e/module-search-freeze-079.spec.ts`（2 条，在真实应用里输入关键词并断言页面没变空白、渲染进程无报错、导图命中确实产出）、`tests/e2e/resource-tile-drag-visual-078.spec.ts` 增加落点底色用例。
- 反向验证：把「取 .text」改回「对象当字符串」→ 单元 1 条 + e2e 2 条失败，且 e2e 精确复现「页面变空白」；把落点改回铺底色 → e2e 实测 `background: rgba(109, 69, 245, 0.12)` 并失败。

## 0.7.8 - 2026-09-25

- 修复**网页导出（HTML）的导图文字没有用应用字体**的问题。用户反馈「html 格式文件打开后，字体需要默认为应用字体」。
  - 根因：EdrawMind 的 HTML 导出把字体写在 `<style>` 的 CSS 类里，用的是**系统字体名**——实测同一份文件里就有 `苹方 粗体`、`.萍方-简`（原文如此，带前导点）、`微软雅黑`、`方正黑体简体` 四种。这些名字在 Windows 上要么匹配不到、要么落到字形不同的替代字体上，于是导图文字与界面明显不一致。
  - 现在解析时把这些 `font-family` 统一换成应用内置的字体栈（与 `.emmx` 解析、界面用的是同一套）。**只换字体**，颜色、字号、字重是用户在导图里设的，原样保留。
- 修复**拖拽资料时出现整块底色和全名提示框**的问题。用户反馈「拖拽还是有整块的底色，我想要的效果是只有图标 + 文件名称，别的杂七杂八的底色都不要，也不需要底部白字黑底的全名称显示框」。
  - 底色来自两处：卡片悬停时会铺一层 `panel2` 底色 + 一圈边框（拖动时鼠标正压在卡片上，那一整块灰就一直跟着走）；「文件找不到」还会给整张卡片画一圈红框。现在悬停/拖拽都不铺底色、不画边框，只保留「操作按钮淡入」这一处反馈。
  - 全名提示框来自卡片自己的 `data-tip`：鼠标一压上去就冒出来。现在提示改挂在**文件名外面那层包装**上，只有把鼠标停在文件名那一小块时才弹出——需要看被省略号截断的全名时依然能看到，但拖动时不会再冒出来挡视线。
  - 顺带修掉一个连带问题：文件名移进包装层后不再是 flex 项，`width/overflow/text-overflow` 会失效，长标题会撑破卡片；已显式改成 `display: block`。
- 新增测试：`tests/mindmap-html-font-078.test.ts`（7 条，字体替换，含真实导出里的四种系统字体名）、`tests/e2e/resource-tile-drag-visual-078.spec.ts`（4 条，在真实界面里量底色与提示框）。反向验证：不换字体 → 4 条失败；恢复底色 → 悬停实测 `background: rgb(238, 240, 244)` 并失败。

## 0.7.7 - 2026-09-25

- 修复**把某个模组的资料全部拖走后，那个模组条目直接消失**的问题（严重）。用户的实际操作：新建一个 A 模组条目 → 把 A 的资料全部拖入「未归属模组」→ A 条目不见了。
  - 根因是**两个字段没同步清理**：0.7.0 用 `hiddenResourceModules` 记「已移除的分组」，0.7.1 改名成 `hiddenResourceGroups`，读取时会把旧字段合并进来（为了兼容升级）。但清理逻辑只更新新字段，**旧字段里的条目每次读取又被合并回来**，于是那个分组永远清不掉「已移除」标记，它只是靠「有资料就显示」这条例外规则在显示——资料一被拖空，例外失效，分组立刻消失。
  - 用用户提供的真实数据库复现并验证：`hiddenResourceGroups` 与 `hiddenResourceModules` 里是同一个模组 id，而该模组名下还有 1 条资料。修复后启动时即清掉该标记，拖空后分组保留，重启后依然保留。
  - 现在 `getSettings()` 合并旧字段后**立即把它清空**（数据收敛，不再反复合并），`reconcileResourceGroups()` 也一并处理旧字段。
- 修复**Ctrl + 滚轮快速缩放时画面突然跳到别处**的问题（严重）。
  - 根因：0.7.6 在 `setZoom` 的更新函数里各自排一个 `requestAnimationFrame`。快速滚动时好几个 wheel 事件会在 React 重渲染**之前**接连触发，每个都按**旧的** `scrollWidth` 算锚点比例、又各排一个 rAF，随后依次执行、互相覆盖，用的还是过期比例。而且在 state 更新函数里做副作用本身就违反 React 约定。
  - 现在锚点记在 ref 里，由 `useLayoutEffect` 在缩放**真正落地后**统一校正一次：它在 React 写完 DOM 之后、浏览器绘制之前同步执行，读到的尺寸已是新值，校正也发生在绘制前，因此既不跳也不闪。
- 修复**长按左键拖动画布时变成框选文字、整个画布跟着鼠标乱晃**的问题（严重）。
  - 两个原因：`mousedown` 没有 `preventDefault`，浏览器同时启动了**原生文字选择**；鼠标移出窗口后收不到 `mousemove`/`mouseup`，拖拽就卡住。
  - 改用 Pointer Events + `setPointerCapture` 把事件锁定在画布上（指针移出窗口也能继续拖动），并补上 CSS `user-select: none` 作为第二道防线。
- 修复**导图里插入的图片和节点图标不显示**的问题。
  - 这两类图形是**嵌套在节点内部的**（`<Shape Type="Image">` / `<Shape Type="Mark">`），而 0.7.5 把嵌套图形一律当成「附属装饰」跳过了。
  - 图片存在 zip 的 `media/` 里，页面通过 `rels/page_rels.xml` 的 `rId` 引用；现在解析时读出来转成 data URI 内嵌，导出的 SVG 自包含、换台电脑也能看。
  - 节点图标（星标、进度）在 `.emmx` 里**只有名字**（`<MarkData Name="star1"/>`），图形数据不在文件里。已把 EdrawMind 用到的四种图标（star1/star2/star3/finished）从它自己的 HTML 导出里内置一份，与原样显示一致。
  - 坐标换算：嵌套图形的 `CX/CY` 是**相对父框左上角**的偏移。实测图标恒为 `CX=17.5/CY=14.3`，换算后是 `(9, 5.8)`，与 EdrawMind HTML 里父节点内的 `translate(9,5.8)` 完全一致。
- 修复**资料汇总里「归属模组」下拉弹窗与应用整体风格不一致**的问题。
  - 根因：原生 `<select>` 的**展开列表由操作系统绘制**，CSS 完全管不到——高亮条是系统蓝、字体是系统字体、圆角阴影也都不是应用那一套。
  - 改成自绘下拉组件，收起态是与输入框同款的按钮，展开态复用已有的 `.combo-list` 样式（与「按名称联想」同一套观感）。键盘操作（上下键、Enter、Home/End、Esc）与点击外部收起都补齐了。
- 修复**拖拽资料时操作图标压住卡片内容**的问题。
  - 那排操作按钮（刷新/编辑/文件夹/×）是绝对定位、高 24px、`top:4px`，而卡片上内边距只有 10px、预览区从 11px 开始——悬停时按钮**必然压住下面的文件图标**，实测重叠 **18px**。
  - 现在卡片恒定预留顶部 30px，按钮只做淡入淡出（`opacity`），不再靠改变尺寸来显隐。顺带修掉「卡片大小偶尔不一样」：悬停/拖拽经过时布局不再变动。
- 新增测试：`tests/mindmap-zoom-pan-077.test.ts`（8 条，缩放锚点与拖动）、`tests/emmx-images-marks-077.test.ts`（14 条，图片与图标）、`tests/resource-select-077.test.ts`（6 条，自绘下拉）、`tests/resource-groups-077.test.ts`（9 条，分组保留，含用户真实库的状态复刻）、`tests/e2e/resource-tile-layout-077.spec.ts`（3 条，卡片布局实测）。
- 反向验证（把代码改回旧写法，确认测试真的能拦住）：缩放锚点 2 条失败、拖动防选中 2 条失败、图片与图标 7 条失败（坐标换算另有 3 条）、卡片重叠实测 `overlap: 18` 失败、分组保留 1 条失败。

## 0.7.6 - 2026-09-24

- 修复导图里**概括括号被填成实心色块**的问题。用户的描述很准确：「感觉像在 Illustrator 里画线条，描边和填充的区别」。
  - 根因：概括括号（Summary）在 `<Geometry>` 上写了 `NoFill="1"`（明确表示不填充），但 `<FillFormat>` 里仍留着与描边同色的绿（`#ff00af54`）。我们此前只看 `<FillFormat>` 有没有颜色，于是把细括号填成了实心绿。
  - 全盘核对 44 个真实导图：**74 个概括括号全部 `NoFill="1"` 且 `Closed="0"`**，EdrawMind 自己的 HTML 导出一律 `fill="none"`；而 25 个分组框（Boundary）是 `Closed="1"`，才真的铺底色（`#e7f2e9`）。现在按 `NoFill` / `Closed` 判断，不再看颜色。
  - 分组框照旧铺底色，两种图形各按各的来。
- 导图文字改用**应用自己的字体**（内置的苹方 / SF Pro），不再是写死的微软雅黑。
  - 此前同一个程序里界面是苹方、导图却是雅黑，中文字形与字重都对不上。字体本身没有更换，只是让导图也用界面那一套内置字体（不依赖系统装没装）。
- 滚轮行为改为**滚轮正常上下滚动，Ctrl + 滚轮缩放**。
  - 根因有两层：一是缩放改了画布尺寸、滚动位置却没跟着调，看起来就是「缩放同时影响滚动条」；二是 **React 17 起把 `onWheel` 注册成 passive 监听**，在它里面调用 `preventDefault()` 是无效的，所以「拦下滚轮、只缩放」这件事根本没生效，原生滚动照常发生。现改用 `addEventListener(..., { passive: false })`，`preventDefault` 才真的能拦住。
  - 缩放以鼠标位置为锚点：光标下的那一点会留在原地，放大后画面不会「跑掉」。工具栏上写了「Ctrl + 滚轮缩放」，免得以为缩放坏了。
- 顺带修掉拖拽资料**随机失效**的问题（0.7.2 起 e2e 里那两个用例会随机有一个失败）。
  - 根因是两处竞态：`dragging` 用 state 存，`dragstart` 到 `dragover` 之间 React 可能还没重渲染，回调里读到 `undefined` 就直接返回、没调 `preventDefault`，浏览器认为「这里不接受放置」而丢掉 drop；另外 `dragstart` 也没往 `dataTransfer` 里写数据，Chromium 对「没有数据的拖拽」会当成无效操作。现改用 ref 同步记录，并写入 `dataTransfer`。
  - 测试侧同时修好：Playwright 的 `dragTo` 只做「按下 → 一次移动 → 松开」，Chromium 有时不认；且上一次落点的异步重排没结束就开始下一次拖拽也会失效。现在分多步移动、等界面真的安静下来再拖，并在手势完全没被浏览器接受时重试（若拖拽确实发生却没搬动资料，仍然判失败）。
- 新增测试：单元测试 `tests/emmx-summary-font-wheel-076.test.ts`（12 条，覆盖括号填充、字体、滚轮三条），e2e 增加节点框重叠、普通滚轮/Ctrl 滚轮、概括括号不填色、导图字体、组内拖拽排序等用例。已用「改回旧写法」反向验证：单元测试 6 条失败；把分组接受放置的守卫去掉，e2e 3 条失败。

## 0.7.5 - 2026-09-24

- 修复导图**折叠的分支仍然被画出来、压在其他节点上**的问题。这是「文字框重叠」反复没修干净的真正原因——0.7.1 修的是行距、0.7.4 修的是行数，而这次跟文字无关：**是节点的位置本身重叠**。
  - 根因：EdrawMind 折叠一个分支时**不会删掉那些节点**，只给它们加上 `<TogglerID>` 标记并在界面上隐藏，坐标还停在折叠前的位置。我们此前把这些节点照常画了出来，于是它们压在正常显示的节点上。
  - 用 EdrawMind 自己导出的 HTML 当标准答案逐节点核对：877 个节点里带 `<TogglerID>` 的 265 个，与导出里的 `display:none` **完全一一对应**（误判 0、漏判 0）。连线同理（594 条可见、260 条随折叠隐藏）。
  - 效果：`世界回归进行曲` 的可见节点重叠从 **327 对降到 0 对**；`渊娲之海` 8 → 0；另外 8 个文件同样归零。龙台掠雪、锈蚀纪元等 13 个本来就没有折叠分支的导图**一个节点都没动**。
  - 折叠的节点右侧会画一个圆圈数字（如截图里的「李恩熙 ③」），显示折叠了几个子节点，避免内容看起来「凭空消失」；数字与 EdrawMind 显示的 18 个折叠点逐一核对一致。
  - 折叠的内容**仍然完整保留在大纲、节点搜索和大纲导出里**——折叠只是不显示，不是丢失。
- 顺带修掉一个会让上面这个修复失效的解析 bug：此前用非贪婪正则 `<Shape ...>([\s\S]*?)</Shape>` 拆图形，而 EdrawMind 会把**星标嵌在节点内部**且排在节点自己的 `<LevelData>` 之前，于是匹配在星标结束处就截断了。实测 `世界回归进行曲` 有 23 个节点受影响：22 个丢了 `<Super>`（大纲里变成假根节点）、20 个丢了 `<SubLevel>`（子节点接不回父节点）、**3 个丢了 `<TogglerID>`**（折叠后仍被画出来，正是残留的 2 对重叠）。现改为按嵌套层级配对拆分，星标这类内部装饰不再当成独立节点。
- 画布包围盒只按**可见**内容计算。折叠分支里有 82 个图形落在可见范围之外（最远的到 y=9106），算进去会把画布撑出一大片空白；EdrawMind 自己的导出也是按可见内容出图。
- 界面上的「N 个节点」只统计画出来的节点，数字与看到的内容一致。
- 新增回归测试 `tests/emmx-fold-075.test.ts`（18 条）：折叠标记识别、不画折叠节点、折叠徽标计数（含「不把连线算成子节点」）、大纲保留折叠内容与层级、嵌套星标不被截断，以及用 EdrawMind 的 HTML 导出做**逐节点、逐折叠点数**的比对。已用「把代码改回旧口径」的方式反向验证——旧代码下 7 条用例失败，并精确指出残留的 2 对重叠（1565×1651、1567×1651）。

## 0.7.4 - 2026-09-24

- 修复导图节点文字**溢出节点框、压到相邻节点上**的问题（0.7.1 修的是行距，这次是**行数**算错了，所以看起来像没修干净）。
  - 根因：EdrawMind 会把**同一行**文字按格式差异切成好几个 `<tp>` 片段，而 `<pp>` 才是真正的一行。此前按 `<tp>` 收集，单 `<pp>` 多 `<tp>` 的节点被算成 2～3 行，需要的高度翻倍，文字就被挤出框、盖住上面的节点。
  - 用真实文件核对：`世界回归进行曲` 的节点 2632 里是
    `<pp><tp>应同渊娲一样，为</tp><tp>某个计划</tp><tp>的产物</tp></pp>`，
    这是**一行**「应同渊娲一样，为某种计划的产物」，此前被算成 3 行、需要 40.5 像素，而文字框只有 15.2 像素——这正是截图里压到上下两个节点上的那处。现在按 `<pp>` 分段后为 1 行，全文件高度溢出节点从 3 个降到 **0 个**。
  - 同一处口径也用在文字标签与大纲提取上，三处保持一致。
- 新增回归测试 `tests/emmx-lines-073.test.ts`：验证「单 `<pp>` 多 `<tp>` 算一行」、全文件行数与 `<pp>` 数一致、任何节点的文字都放得进自己的文字框。已用「把代码改回旧口径」的方式反向验证过——旧代码下 4 条用例全部失败并精确指出溢出的 3 个节点，确认测试真的能拦住这个 bug。
- 完整性核对测试（`tests/coverage-audit.test.ts`）的独立提取口径同步改为按 `<pp>` 分段，与解析器一致。

## 0.7.3 - 2026-09-24

- 打包时同时产出两种文件，方便分发给不同用途：
  - `COC跑团记录簿-<版本>-win-x64.exe` —— **免安装版**，双击即用，数据存在程序旁边的 `data/` 目录，整个文件夹拷走就是全部数据（自己用推荐这个）。
  - `COC跑团记录簿-<版本>-Windows-x64-Setup.exe` —— **安装包**，走安装向导、可自选安装位置、自动创建桌面与开始菜单快捷方式，数据存在用户目录，卸载重装不会丢（发给朋友测试推荐这个）。
  两者体积只差约 0.3 MB。此前命令行里写死了 `portable`，把配置里的安装包目标覆盖掉了，所以一直只出免安装版；现在不再写死，由配置决定产物。
- `tools\公司电脑-一键跑测试并打包.bat` 同步改为产出两种文件并列出名称。

## 0.7.2 - 2026-09-24

- 修复把 A 分组的资料全部拖到 B 分组后、A 分组条目直接消失的问题。现在拖空的分组会保留为「空分组」，符合「资料搬走了、分组还在」的直觉。
  根因是两条规则打架：「已从本页移除」的标记只对空分组生效，有资料的分组靠例外规则显示；而分组可能仍带着早先留下的移除标记，于是它只是「靠例外才显示」的，资料一被拖走例外就失效，分组随之消失。现在从源头杜绝——**还有资料的分组一律拒绝被标记为已移除**，并在启动时自动纠正历史数据里已存在的不一致状态（例如从备份恢复、或直接改过数据库留下的）。

## 0.7.1 - 2026-09-24

- 修复线条被画得「曲里拐弯」的问题。此前为了让连线接到节点框上会搬动连线端点、甚至平移整条干线，结果把 EdrawMind 原本横平竖直的折线拉成了斜线与鼓包。现在改为**完全保留导图文件里的原始线条形状**（父节点出一小段圆角、沿竖直干线延伸、分支点用小圆角转向子节点、子节点之间是短横直线），只在端点确实差一小段**水平或垂直**距离时补一条短线接上。用三个真实导图核对：终点与原始几何 100% 吻合，补接线全部是水平或垂直的，不再出现任何长斜线。
- 修复「资料汇总」页整页空白、连添加按钮都消失的问题。原因是「已从本页移除」的标记被用来隐藏**还有资料的分组**：把模组分组与未归属分组都删掉后，仅有的资料全在被隐藏的模组下，于是页面一片空白。现在该标记只对空分组生效——有资料的分组一定显示；空白引导页与分组列表也改用同一个判断，不再出现「容器在、里面却空无一物」的情况；归属到已不存在模组的资料会自动归入未归属分组，保证任何资料都有地方显示。
- 修复导图长文本溢出、压到相邻节点的问题。此前行距按字号的 1.25 倍计算，实测「感受到一种病态的美学崇拜」这一段 5 行需要 62.5 像素，而文字框只有 55.3 像素，多出的 7.2 像素直接盖住下方节点。按 EdrawMind 的真实排版反推，行距应为 1.1 倍；另外文字框偶尔装不下全部文字（EdrawMind 按单行给了框高），这时改在节点框内垂直居中，节点框才是画面上真正的边界。
- 修复导图里的引号显示成 `&quot;` 字样的问题：导图文件里存的是 XML 实体，此前当普通文字处理，既在画面上显示成实体原文，又让宽度估算虚高、把本该放下的长句误判为超宽而折行。
- 导图节点搜索对齐浏览器查找：画布上全部命中黄底高亮、当前命中橙底高亮（此前只在结果列表里标了当前项，画布上没有任何标记）。结果列表里也把命中的关键词标出来。
- 点击搜索结果改为把命中处摆到画布正中。此前按「命中序号占总数的比例」估算纵向位置，既不是元素真实位置、也没考虑缩放，所以经常在画布上找不到。现在按元素的实际像素位置计算，缩放多少都成立。
- 「未归属模组」分组现在可以被真正删除。此前它没有模组 id，删除时记不下任何标记，刷新后必然复活，右下角也只提示「已移除资料」。现在它与模组分组共用同一套机制，删除后不再显示；一旦又有资料变成未归属，分组会自动回来，免得资料无处可去。
- 资料图标的悬停提示改用应用风格：不再是系统默认的黑底方块，而是与应用一致的深色圆角浮层，深色主题下自动反色。
- 资料卡片的操作图标由三个改为四个，大小一致、等宽占满卡片顶部：更新 / 编辑 / 打开文件所在位置 / 移除。「编辑」用源程序打开（导图交给 EdrawMind、文档交给 Word、图片交给默认看图器），也就是原来文件夹图标的功能；文件夹图标改为「打开文件所在位置」，只打开文件夹并选中文件，不启动任何程序。链接类资料没有本地文件，该位置留空以保持四格对齐。

## 0.7.0 - 2026-09-24

- 修复导图里节点文字错位、互相重叠的问题，根因有两个：一是每个节点其实都带自己的文字区域（坐标相对节点框左上角，尺寸通常小于节点框），此前只按「居中于节点框」绘制，位置一直有偏差；二是 EdrawMind 按框宽自动折行、文件里只存原始段落，而 SVG 的文字不会自动折行，长句直接溢出压到相邻节点。现在按节点自己的文字区域定位，并按区域宽度自动折行。
- 导图查看器打开时自动定位到内容中心。此前停在画布左上角，而内容往往在画布中部（例如 7257×5928 的画布，内容从 x≈1100 开始），打开只看到大片空白，得自己缩放拖拽才能找到内容。
- EdrawMind 网页导出（.html）的子页面按钮改用画布的真实名称（存在 `ed:name` 里，如「主内容」「待探索调查」「卷宗」「冬蛾相关」），不再是「画布 0」这样的序号。
- 导图查看器的缩放百分比移到「缩小」按钮左侧，并给固定宽度，缩放时不再推挤搜索框；工具条上的短按钮不再被挤成两行。
- 「未归属模组」分组补上删除按钮：它是最后的兜底分组，没有「取消归属」这个中间态可去，所以只有「移除」一个动作（不删除原文件）。清空后若页面上再无任何分组与资料，回到「还没有资料」的空白引导页；只要还有模组在，分组照常显示，方便把资料拖回去。
- 「资料汇总」从模组卡片里的小区块提升为左侧导航的一级页面，位置在“调查员角色卡”与“跑团闲记”之间；右上角以主按钮形式提供“+ 导图 / + 链接 / + 文件”，与“+ 新建模组”同一套样式，页面内不再重复放一排按钮。
- 资料按模组分组显示，模组名可折叠展开，分组右侧的“×”可删除该分组：会先问用户是「只取消归属」（资料移到未归属）还是「连资料一起移除」，两种都不会删除原文件。
- 归属改为「新建时点选 + 拖拽调整」：卡片底部不再常驻归属下拉，视觉上干净许多。把卡片拖到别的分组即可改归属，拖到同组另一张卡片上即可调整顺序；落点分组会高亮。
- 资料卡片改为桌面图标样式：图片显示缩略图、其他文件显示系统真实图标（docx 显示 Word 图标、pdf 显示 PDF 图标等），链接用与应用一致的线条图标。鼠标悬停时右上角出现三个小按钮：更新、用原程序打开、从汇总里移除。
- 「更新」按钮让用户重新指定这条资料对应的文件，用于内容更新或文件挪了位置。
- 修复图片资料显示成破图的问题：此前图片走的是闲记的 `coc-media` 协议，而那个协议只允许访问程序自己的数据目录，资料图片在用户自己的目录里，请求全部失败。现在改用专门的只读通道。
- 导图查看器新增节点搜索框（位于工具条左侧）：输入关键词即列出命中的节点文字，可逐条跳转。
- 导图查看器支持滚轮缩放与按住左键拖拽平移，长图与宽图都能顺手查看。
- 导图查看器右上角的按钮统一成同一套外观，关闭键不再比其他按钮小一圈；图形区不再让按钮边框印到画布上。
- 「大纲 878」按钮更名为「大纲显示」。
- 修复导图里关系连线的说明文字完全丢失的问题：EdrawMind 把「意图杀害」「母女」「死敌」这类标注存在 `RelatConnector` 里，其文字坐标是相对连线锚点的偏移，此前既没读文字、也没叠加锚点，导致标注和概括归纳的线条都看不见。现在按「锚点 + 偏移」还原，分组框标题一并补上。
- 新增对 EdrawMind 网页导出（.html）的支持：每个子页面就是一个现成的矢量画布，图形由 EdrawMind 自己渲染，完整且可完全离线显示；底部提供子页面切换按钮，切换时自动适应宽度。新版 `.emmx`（EdrawMind 12 起）的画布是私有二进制、无法可靠还原图形，添加这类文件时会明确提示「请从 EdrawMind 导出 HTML 后添加」，并可直接跳去打开 EdrawMind。
- 资料表单里的「归属模组」下拉改用与应用一致的外观，不再是系统默认的灰色控件。
- 数据库升级到 schema v7：资料可以不归属任何模组。升级前会自动备份为 `coc.sqlite.before-v7.bak`。

## 0.6.8 - 2026-09-23

- 模组下新增「资料汇总」，可以把 EdrawMind 思维导图（.emmx）、Notion 等网页链接、以及其他本地文件（PDF/Word/图片等）挂到对应模组上。软件只登记路径、不复制文件：原文件改了立刻生效，被移走时那条资料会标出「文件找不到」并提示重新指定。
- 导图支持在软件内预览，且预览是**矢量图**而不是文件自带的缩略图。导图文件里的缩略图只有 210 像素左右，而实际内容区动辄 5000 像素以上（实测「龙台掠雪」为 5486×4913），放大后完全糊成马赛克；现在改为读取导图内部的画布数据重新绘制，可以自由放大到看清每一个字，多画布（子页面）也能逐个切换。
- 导图里提取出的全部节点文字会纳入模组的「全模组正文搜索」，搜一个词能同时命中场次正文和导图节点；导图结果显示命中节点数与节点原文。
- 编辑仍在原软件里进行：每条资料都有「用 EdrawMind 打开」（或对应程序）按钮，软件本身不修改任何原文件。
- 数据库升级到 schema v6：新增模组资料表，删除模组时其资料一并清除。升级前会自动备份为 `coc.sqlite.before-v6.bak`。

## 0.6.7 - 2026-09-23

- 右上角的窗口控件改为 Windows 10 的软件窗口控件风格：三个矩形按钮平铺在标题栏右端，图标为 10×10 细线（最小化是横线、最大化是方框、关闭是叉），无圆角无边框；鼠标悬停时按钮底色变浅灰，关闭键变红底白字；窗口最大化后最大化键自动换成“还原”的双框图标。
- 标题栏高度由 46px 收窄到 32px（Windows 10 的标准高度），控件随之由 46×46 变为 46×32，界面看起来更紧凑；标题栏高度改为单一变量，下方内容区高度跟着它一起走。

## 0.6.6 - 2026-09-23

- 修复导入表格后新增场次平白弹出“选择下一场编号”的问题。此前场次编号与场次名各存一份：导入只按表格里的“场次名”改名，编号原样不动，于是出现“场次名叫第 10 场、编号却还是 17”这种分家状态。界面按编号判断编号是否连续，就会认定缺了 10-16，在新增场次时要求用户确认——而用户看到的场次名明明是连续的。现在编号统一以场次名里的“第 N 场”为准：新建、改名、导入与升级迁移都按名称取号，两者不会再各说各话。
- 数据库升级到 schema v5：升级时按场次名把已有场次的编号校正回一致（名称里没写编号、或目标编号已被别的场次占用时不改动，绝不写重号）。升级前会自动备份为 `coc.sqlite.before-v5.bak`。
- 导入表格后按模组统一校正一次编号，因此“两场编号互换”这类情况也能正确换过来。
- 场次名里没有编号（例如用户自己起的“决战夜”）时保持原有编号不变，新增场次仍按“现存最大编号 + 1”接续。

## 0.6.5 - 2026-09-23

- 修复跑团闲记图片查看器的三个问题：浅色模式下“适应窗口”按钮白字白底完全看不清（头部整体是白字，按钮背景又取了浅色主题的白色面板）；“适应窗口”与关闭键一大一小（36px 与 30px），现统一为 30px；“适应窗口”要点很多次、或把鼠标挪到右上角才生效——查看器盖住了窗口顶部 46px 的标题栏，而标题栏是系统级拖拽区，会把点击当成拖动窗口吃掉，现已把查看器整块声明为非拖拽区。
- 场次详情弹窗的“在当前场次正文内搜索”此前只显示命中次数，看不出命中了什么。现在改为与“全模组正文搜索”同一套结果列表：每条命中给出发言人小标题、关键词高亮和前后一小段原文摘要，点某一条就滚动定位到正文里对应的那条消息，被定位的那条左侧显示竖线标记；结果多时列表自身可滚动查看。搜索框保持单行原样，命中数仍在输入框右侧。从模组搜索点进场次时，关键词会自动带过去，并直接定位到该场次里第一条命中的消息。
- 单份导出的带图 DOC、对话 DOC、DOCX、PDF 不再带模组封面（模组名与 KP、PC/PL 名单），封面只保留在合成文件里；场次标题照常保留。

## 0.6.3 - 2026-09-23

- 模组新增“跑团状态”：编辑模组时在模组名右侧点选，可选已完成、进行中、未开始；新建模组时不预选，必须明确选择才能保存。模组列表里在“N 场”徽章旁同步显示该状态。
- 模组列表的 KP / PC / PL 一栏右侧新增“全模组正文搜索”，可在该模组所有场次的正文中查找关键词；结果按场次列出命中处数与关键词前后的原文摘要，关键词高亮，点击直接打开该场次详情。没有正文的场次（链接未检测）不会命中。
- 跑团闲记卡片高度加高一倍，正文显示行数同步放宽，长内容不再被过早截断。
- 修复编辑跑团闲记时旁边重复出现同一张卡片的问题：此前正在编辑的那条会被再渲染一遍，看起来像多出一个“修改前版本”。
- 数据库升级到 schema v4：已有模组自动补为“未开始”，备份恢复同样兼容旧备份。
- 空白导入模板新增“跑团状态”列（可填未开始/进行中/已完成，留空按未开始处理），并在填写说明里交代用法；导出表格同步带上该列，原“状态”列改名“链接状态”以便与跑团状态区分，旧列名继续兼容。跑团状态只对本次新建的模组生效，不会覆盖软件里已设的状态。
- 修复归档目录失效时下载、批量下载、批量合成与导出表格全部失败却看不出原因的问题：目录不存在或不可写时，现在会明确提示真实路径与失败原因，并在“数据与设置”页标红；同时新增“恢复默认位置”按钮，可一键改回当前用户的「文档\COC跑团记录」。
- 修复跑团闲记插入图片后只显示破碎图标和文件名的问题：内容安全策略此前只允许 `'self'` 与 `data:`，闲记图片使用的 `coc-media://` 协议被拦截。现已在策略中放行该协议，jpg、jpeg、png、gif、webp、bmp 均可正常显示。
- 跑团闲记的图片改为按原始长宽比适配卡片宽度显示，不再固定方框裁切；卡片上的预览图同样保持比例。
- 跑团记录汇总里的“删除”按钮统一改为方形灰色“×”，高度沿用 30px、宽度取同值成为正方形，大小形状颜色与模组编辑界面中的“×”一致。
- 跑团闲记的图片支持双击查看原图：默认等比铺满窗口，可一键切到“原始尺寸”按 1:1 像素查看细节（超出窗口时可滚动），按 Esc 或点空白处关闭。

## 0.6.2 - 2026-09-22

- 修复删除末尾场次后新增场次编号凭空跳号的问题：新增场次的默认编号改为接续“现存场次的最大编号 + 1”。此前编号取自一个只增不减的历史最大值，删掉第 9-16 场后只剩第 8 场时，新增场次会直接跳到第 17 场。
- 编号选择窗口保持不变：现存编号中间存在空缺时仍会弹出，可填补空缺、接续最后编号或自定义编号；已有场次的名称与编号不会被重排或改名。
- 导入表格时链接状态一律重置为“待检测”，不再采信表格里的历史状态；覆盖更新已有场次时会同时清空已抓取的正文、抓取时间与错误信息，必须重新检测才能抓取正文。手动粘贴正文的场次保持“手动内容”不受影响。
- 导出的表格继续保留“状态”列以便查看，但导入时不再读取该列；跑团日期仍会原样带回。

## 0.6.1 - 2026-09-22

- 修复窗口仅能上下缩放、左右与四角缩放失效且拖动延迟的问题：关闭 Windows 隐形原生边框，恢复界面内八方向缩放热区，窗口保持直角、无阴影残块。
- 统一导入与导出表格格式：软件导出的表格可直接重新导入，状态、跑团日期、最近抓取时间三列会原样带回；重复海豹链接的场次按表格内容覆盖更新。
- 空白导入模板同时提供 xlsx 与 CSV 两种格式，默认含 1 位 KP 与 3 对 PC/PL（PC1/PL1、PC2/PL2、PC3/PL3）位置，表格下方附填写说明；旧版“参与者”一列的写法继续兼容。
- 批量下载、批量合成、导出表格完成后自动打开存储位置（单份合成/表格会在资源管理器中选中文件，批量下载与多份合成打开归档目录）。
- 批量合成的 TXT/DOCX/PDF 保留封面首页，取消每场强制分页，场次连排并在不同场次之间加入通栏分割线，场次标题保留。

## 0.6.0 - 2026-09-22

- 跑团记录汇总页场次超出可见范围时，右侧显示可正常拖动的竖向滚动条。
- 添加场次时，若现有场次编号不连续（例如删除过场次），会弹出与现有提示风格一致的编号选择窗口，可填补空缺编号、接续最后编号，也可自定义 1-9999 且未被占用的编号。
- 窗口四角改为 Windows 10 风格直角，保留窗口自由调整大小功能，并清理透明窗口遗留的矩形阴影与背景残块。

## 0.1.0 - 2026-09-11

- Portable Windows x64 test build.
- Local module, session record, SeaLog probing, filtering, export, character, table, backup and restore workflows.
- Six single-record exports: raw JSON, image DOC, dialogue DOC, DOCX, TXT and PDF.
- Offline DOCX/PDF generation without Microsoft Word or LibreOffice.
- JSON and ZIP backup/restore with registered-archive safety checks.
- Unsigned build; Windows SmartScreen may warn before launch.
