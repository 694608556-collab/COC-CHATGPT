# Changelog

## 0.7.1 - 2026-09-24

- 修复连线没有接在节点框边上的问题：端点此前常落在框角附近（实测矮节点的端点比垂直中心低 12 像素，正好半个框高，也就是接在右下角），现在统一接在最近那条边的**中点**上，与 EdrawMind 导出 HTML 的效果一致。实测三个真实导图的贴框端点居中率均为 100%。
- 修复连线被画成斜线的问题：EdrawMind 画的是横平竖直的折线，而调整端点位置会把相邻折点甩开、原本水平的段被拉成斜线（实测出现过 73 条）。现在移动端点时会把相连的整条干线一起平移，保持折线形状不变。
- 修复「资料汇总」页整页空白、连添加按钮都消失的问题。原因是「已从本页移除」的标记被用来隐藏**还有资料的分组**：把模组分组与未归属分组都删掉后，仅有的资料全在被隐藏的模组下，于是页面一片空白。现在该标记只对空分组生效——有资料的分组一定显示；空白引导页与分组列表也改用同一个判断，不再出现「容器在、里面却空无一物」的情况；归属到已不存在模组的资料会自动归入未归属分组，保证任何资料都有地方显示。
- 修复导图连线没有接到节点框上的问题。EdrawMind 存的连线端点语义不统一：`BeginPt` 有时存的是节点框中心、有时是框边缘，而连线自身的几何首尾点又常常落在框外一小段（实测 6.5~27 像素不等）。三者混在一起，画出来就是线头浮在框外、看着没接上。现在统一把端点校正到最近的节点框边界上，实测悬空端点由 59 处降到 0。
- 修复导图长文本溢出、压到相邻节点的问题。此前行距按字号的 1.25 倍计算，实测「感受到一种病态的美学崇拜」这一段 5 行需要 62.5 像素，而文字框只有 55.3 像素，多出的 7.2 像素直接盖住下方节点。按 EdrawMind 的真实排版反推，行距应为 1.1 倍；另外文字框偶尔装不下全部文字（EdrawMind 按单行给了框高），这时改在节点框内垂直居中，节点框才是画面上真正的边界。
- 修复导图里的引号显示成 `&quot;` 字样的问题：导图文件里存的是 XML 实体，此前当普通文字处理，既在画面上显示成实体原文，又让宽度估算虚高、把本该放下的长句误判为超宽而折行。
- 修复部分连线完全不显示的问题：连线若从曲线起笔，生成的路径以 `C` 开头而不是 `M`，浏览器判为非法路径、整条线都不画（实测一份导图里有 2 条这样的连线从未显示过）。
- 导图节点搜索对齐浏览器查找：画布上全部命中黄底高亮、当前命中橙底高亮（此前只在结果列表里标了当前项，画布上没有任何标记）。结果列表里也把命中的关键词标出来。
- 点击搜索结果改为把命中处摆到画布正中。此前按「命中序号占总数的比例」估算纵向位置，既不是元素真实位置、也没考虑缩放，所以经常在画布上找不到。现在按元素的实际像素位置计算，缩放多少都成立。
- 「未归属模组」分组现在可以被真正删除。此前它没有模组 id，删除时记不下任何标记，刷新后必然复活，右下角也只提示「已移除资料」。现在它与模组分组共用同一套机制，删除后不再显示；一旦又有资料变成未归属，分组会自动回来，免得资料无处可去。
- 资料图标的悬停提示改用应用风格：不再是系统默认的黑底方块，而是与应用一致的深色圆角浮层，深色主题下自动反色。
- 资料卡片的操作图标由三个改为四个，大小一致、等宽占满卡片顶部：更新 / 编辑 / 打开文件所在位置 / 移除。「编辑」用源程序打开（导图交给 EdrawMind、文档交给 Word、图片交给默认看图器），也就是原来文件夹图标的功能；文件夹图标改为「打开文件所在位置」，只打开文件夹并选中文件，不启动任何程序。链接类资料没有本地文件，该位置留空以保持四格对齐。

## 0.7.0 - 2026-09-24

- 修复导图里节点文字错位、互相重叠的问题，根因有两个：一是每个节点其实都带自己的文字区域（坐标相对节点框左上角，尺寸通常小于节点框），此前只按「居中于节点框」绘制，位置一直有偏差；二是 EdrawMind 按框宽自动折行、文件里只存原始段落，而 SVG 的文字不会自动折行，长句直接溢出压到相邻节点。现在按节点自己的文字区域定位，并按区域宽度自动折行。
- 导图查看器打开时自动定位到内容中心。此前停在画布左上角，而内容往往在画布中部（例如 7257×5928 的画布，内容从 x≈1100 开始），打开只看到大片空白，得自己缩放拖拽才能找到内容。
- EdrawMind 网页导出（.html）的子页面按钮改用画布的真实名称（存在 `ed:name` 里，如「主内容」「待探索调查」「卷宗」「冬蛾相关」），不再是「画布 0」这样的序号。
- 导图查看器的缩放百分比移到「缩小」按钮左侧，并给固定宽度，缩放时不再推挤搜索框；工具条上的短按钮不再被挤成两行。
- 「未归属模组」分组补上删除按钮：它是最后的兜底分组，没有「取消归属」这个中间态可去，所以只有「移除」一个动作（不删除原文件）。清空后若页面上再无任何分组与资料，回到「还没有资料」的空白引导页；只要还有模组在，分组照常显示，方便把资料拖回去。
- 「资料汇总」从模组卡片里的小区块提升为左侧导航的一级页面，位置在“调查员角色卡”与“跑团闲记”之间；右上角以主按钮形式提供“+ 导图 / + 链接 / + 文件”，与“+ 新建模组”同一套样式，页面内不再重复放一排按钮。
- 资料按模组分组显示，模组名可折叠展开，分组右侧的“×”可删除该分组：会先问用户是「只取消归属」（资料移到未归属）还是「连资料一起移除」，两种都不会删除原文件。
- 归属改为「新建时点选 + 拖拽调整」：卡片底部不再常驻归属下拉，视觉上干净许多。把卡片拖到别的分组即可改归属，拖到同组另一张卡片上即可调整顺序；落点分组会高亮。
- 资料卡片改为桌面图标样式：图片显示缩略图、其他文件显示系统真实图标（docx 显示 Word 图标、pdf 显示 PDF 图标等），链接用与应用一致的线条图标。鼠标悬停时右上角出现三个小按钮：更新、用原程序打开、从汇总里移除。
- 「更新」按钮让用户重新指定这条资料对应的文件，用于内容更新或文件挪了位置。
- 修复图片资料显示成破图的问题：此前图片走的是闲记的 `coc-media` 协议，而那个协议只允许访问程序自己的数据目录，资料图片在用户自己的目录里，请求全部失败。现在改用专门的只读通道。
- 导图查看器新增节点搜索框（位于工具条左侧）：输入关键词即列出命中的节点文字，可逐条跳转。
- 导图查看器支持滚轮缩放与按住左键拖拽平移，长图与宽图都能顺手查看。
- 导图查看器右上角的按钮统一成同一套外观，关闭键不再比其他按钮小一圈；图形区不再让按钮边框印到画布上。
- 「大纲 878」按钮更名为「大纲显示」。
- 修复导图里关系连线的说明文字完全丢失的问题：EdrawMind 把「意图杀害」「母女」「死敌」这类标注存在 `RelatConnector` 里，其文字坐标是相对连线锚点的偏移，此前既没读文字、也没叠加锚点，导致标注和概括归纳的线条都看不见。现在按「锚点 + 偏移」还原，分组框标题一并补上。
- 新增对 EdrawMind 网页导出（.html）的支持：每个子页面就是一个现成的矢量画布，图形由 EdrawMind 自己渲染，完整且可完全离线显示；底部提供子页面切换按钮，切换时自动适应宽度。新版 `.emmx`（EdrawMind 12 起）的画布是私有二进制、无法可靠还原图形，添加这类文件时会明确提示「请从 EdrawMind 导出 HTML 后添加」，并可直接跳去打开 EdrawMind。
- 资料表单里的「归属模组」下拉改用与应用一致的外观，不再是系统默认的灰色控件。
- 数据库升级到 schema v7：资料可以不归属任何模组。升级前会自动备份为 `coc.sqlite.before-v7.bak`。

## 0.6.8 - 2026-09-23

- 模组下新增「资料汇总」，可以把 EdrawMind 思维导图（.emmx）、Notion 等网页链接、以及其他本地文件（PDF/Word/图片等）挂到对应模组上。软件只登记路径、不复制文件：原文件改了立刻生效，被移走时那条资料会标出「文件找不到」并提示重新指定。
- 导图支持在软件内预览，且预览是**矢量图**而不是文件自带的缩略图。导图文件里的缩略图只有 210 像素左右，而实际内容区动辄 5000 像素以上（实测「龙台掠雪」为 5486×4913），放大后完全糊成马赛克；现在改为读取导图内部的画布数据重新绘制，可以自由放大到看清每一个字，多画布（子页面）也能逐个切换。
- 导图里提取出的全部节点文字会纳入模组的「全模组正文搜索」，搜一个词能同时命中场次正文和导图节点；导图结果显示命中节点数与节点原文。
- 编辑仍在原软件里进行：每条资料都有「用 EdrawMind 打开」（或对应程序）按钮，软件本身不修改任何原文件。
- 数据库升级到 schema v6：新增模组资料表，删除模组时其资料一并清除。升级前会自动备份为 `coc.sqlite.before-v6.bak`。

## 0.6.7 - 2026-09-23

- 右上角的窗口控件改为 Windows 10 的软件窗口控件风格：三个矩形按钮平铺在标题栏右端，图标为 10×10 细线（最小化是横线、最大化是方框、关闭是叉），无圆角无边框；鼠标悬停时按钮底色变浅灰，关闭键变红底白字；窗口最大化后最大化键自动换成“还原”的双框图标。
- 标题栏高度由 46px 收窄到 32px（Windows 10 的标准高度），控件随之由 46×46 变为 46×32，界面看起来更紧凑；标题栏高度改为单一变量，下方内容区高度跟着它一起走。

## 0.6.6 - 2026-09-23

- 修复导入表格后新增场次平白弹出“选择下一场编号”的问题。此前场次编号与场次名各存一份：导入只按表格里的“场次名”改名，编号原样不动，于是出现“场次名叫第 10 场、编号却还是 17”这种分家状态。界面按编号判断编号是否连续，就会认定缺了 10-16，在新增场次时要求用户确认——而用户看到的场次名明明是连续的。现在编号统一以场次名里的“第 N 场”为准：新建、改名、导入与升级迁移都按名称取号，两者不会再各说各话。
- 数据库升级到 schema v5：升级时按场次名把已有场次的编号校正回一致（名称里没写编号、或目标编号已被别的场次占用时不改动，绝不写重号）。升级前会自动备份为 `coc.sqlite.before-v5.bak`。
- 导入表格后按模组统一校正一次编号，因此“两场编号互换”这类情况也能正确换过来。
- 场次名里没有编号（例如用户自己起的“决战夜”）时保持原有编号不变，新增场次仍按“现存最大编号 + 1”接续。

## 0.6.5 - 2026-09-23

- 修复跑团闲记图片查看器的三个问题：浅色模式下“适应窗口”按钮白字白底完全看不清（头部整体是白字，按钮背景又取了浅色主题的白色面板）；“适应窗口”与关闭键一大一小（36px 与 30px），现统一为 30px；“适应窗口”要点很多次、或把鼠标挪到右上角才生效——查看器盖住了窗口顶部 46px 的标题栏，而标题栏是系统级拖拽区，会把点击当成拖动窗口吃掉，现已把查看器整块声明为非拖拽区。
- 场次详情弹窗的“在当前场次正文内搜索”此前只显示命中次数，看不出命中了什么。现在改为与“全模组正文搜索”同一套结果列表：每条命中给出发言人小标题、关键词高亮和前后一小段原文摘要，点某一条就滚动定位到正文里对应的那条消息，被定位的那条左侧显示竖线标记；结果多时列表自身可滚动查看。搜索框保持单行原样，命中数仍在输入框右侧。从模组搜索点进场次时，关键词会自动带过去，并直接定位到该场次里第一条命中的消息。
- 单份导出的带图 DOC、对话 DOC、DOCX、PDF 不再带模组封面（模组名与 KP、PC/PL 名单），封面只保留在合成文件里；场次标题照常保留。

## 0.6.3 - 2026-09-23

- 模组新增“跑团状态”：编辑模组时在模组名右侧点选，可选已完成、进行中、未开始；新建模组时不预选，必须明确选择才能保存。模组列表里在“N 场”徽章旁同步显示该状态。
- 模组列表的 KP / PC / PL 一栏右侧新增“全模组正文搜索”，可在该模组所有场次的正文中查找关键词；结果按场次列出命中处数与关键词前后的原文摘要，关键词高亮，点击直接打开该场次详情。没有正文的场次（链接未检测）不会命中。
- 跑团闲记卡片高度加高一倍，正文显示行数同步放宽，长内容不再被过早截断。
- 修复编辑跑团闲记时旁边重复出现同一张卡片的问题：此前正在编辑的那条会被再渲染一遍，看起来像多出一个“修改前版本”。
- 数据库升级到 schema v4：已有模组自动补为“未开始”，备份恢复同样兼容旧备份。
- 空白导入模板新增“跑团状态”列（可填未开始/进行中/已完成，留空按未开始处理），并在填写说明里交代用法；导出表格同步带上该列，原“状态”列改名“链接状态”以便与跑团状态区分，旧列名继续兼容。跑团状态只对本次新建的模组生效，不会覆盖软件里已设的状态。
- 修复归档目录失效时下载、批量下载、批量合成与导出表格全部失败却看不出原因的问题：目录不存在或不可写时，现在会明确提示真实路径与失败原因，并在“数据与设置”页标红；同时新增“恢复默认位置”按钮，可一键改回当前用户的「文档\COC跑团记录」。
- 修复跑团闲记插入图片后只显示破碎图标和文件名的问题：内容安全策略此前只允许 `'self'` 与 `data:`，闲记图片使用的 `coc-media://` 协议被拦截。现已在策略中放行该协议，jpg、jpeg、png、gif、webp、bmp 均可正常显示。
- 跑团闲记的图片改为按原始长宽比适配卡片宽度显示，不再固定方框裁切；卡片上的预览图同样保持比例。
- 跑团记录汇总里的“删除”按钮统一改为方形灰色“×”，高度沿用 30px、宽度取同值成为正方形，大小形状颜色与模组编辑界面中的“×”一致。
- 跑团闲记的图片支持双击查看原图：默认等比铺满窗口，可一键切到“原始尺寸”按 1:1 像素查看细节（超出窗口时可滚动），按 Esc 或点空白处关闭。

## 0.6.2 - 2026-09-22

- 修复删除末尾场次后新增场次编号凭空跳号的问题：新增场次的默认编号改为接续“现存场次的最大编号 + 1”。此前编号取自一个只增不减的历史最大值，删掉第 9-16 场后只剩第 8 场时，新增场次会直接跳到第 17 场。
- 编号选择窗口保持不变：现存编号中间存在空缺时仍会弹出，可填补空缺、接续最后编号或自定义编号；已有场次的名称与编号不会被重排或改名。
- 导入表格时链接状态一律重置为“待检测”，不再采信表格里的历史状态；覆盖更新已有场次时会同时清空已抓取的正文、抓取时间与错误信息，必须重新检测才能抓取正文。手动粘贴正文的场次保持“手动内容”不受影响。
- 导出的表格继续保留“状态”列以便查看，但导入时不再读取该列；跑团日期仍会原样带回。

## 0.6.1 - 2026-09-22

- 修复窗口仅能上下缩放、左右与四角缩放失效且拖动延迟的问题：关闭 Windows 隐形原生边框，恢复界面内八方向缩放热区，窗口保持直角、无阴影残块。
- 统一导入与导出表格格式：软件导出的表格可直接重新导入，状态、跑团日期、最近抓取时间三列会原样带回；重复海豹链接的场次按表格内容覆盖更新。
- 空白导入模板同时提供 xlsx 与 CSV 两种格式，默认含 1 位 KP 与 3 对 PC/PL（PC1/PL1、PC2/PL2、PC3/PL3）位置，表格下方附填写说明；旧版“参与者”一列的写法继续兼容。
- 批量下载、批量合成、导出表格完成后自动打开存储位置（单份合成/表格会在资源管理器中选中文件，批量下载与多份合成打开归档目录）。
- 批量合成的 TXT/DOCX/PDF 保留封面首页，取消每场强制分页，场次连排并在不同场次之间加入通栏分割线，场次标题保留。

## 0.6.0 - 2026-09-22

- 跑团记录汇总页场次超出可见范围时，右侧显示可正常拖动的竖向滚动条。
- 添加场次时，若现有场次编号不连续（例如删除过场次），会弹出与现有提示风格一致的编号选择窗口，可填补空缺编号、接续最后编号，也可自定义 1-9999 且未被占用的编号。
- 窗口四角改为 Windows 10 风格直角，保留窗口自由调整大小功能，并清理透明窗口遗留的矩形阴影与背景残块。

## 0.1.0 - 2026-09-11

- Portable Windows x64 test build.
- Local module, session record, SeaLog probing, filtering, export, character, table, backup and restore workflows.
- Six single-record exports: raw JSON, image DOC, dialogue DOC, DOCX, TXT and PDF.
- Offline DOCX/PDF generation without Microsoft Word or LibreOffice.
- JSON and ZIP backup/restore with registered-archive safety checks.
- Unsigned build; Windows SmartScreen may warn before launch.
